You have completed fast packet assessments for {{turn_packet_count}} tool call(s) in turn {{current_turn_id}}.

{{analysis_focus_instructions}}

Summarize how well the model progressed toward answering the user request during this turn.
- Keep the result compact and aggregation-friendly.
- If the same tool was used more than once, explain retries briefly in cross_attempt_reconciliation.
- Include in follow_up_candidates only tool_call_part_id values that deserve deeper review.
- Return exactly one JSON object and no prose.
- Repeated tools in this turn: {{repeated_tools}}.

Return exactly one JSON object with this shape:
{
  "turn_id": "{{current_turn_id}}",
  "total_tool_calls_assessed": {{turn_packet_count}},
  "turn_outcome": "answered" | "partially_answered" | "not_answered" | "unclear",
  "turn_outcome_rationale": "<short rationale>",
  "per_tool_findings": [
    {
      "tool_call_part_id": "<tool-call-part-id>",
      "tool_name": "<tool-name>",
      "result_status": "successful" | "partially_successful" | "unsuccessful" | "request_error" | "response_error" | "empty" | "unclear",
      "brief_finding": "<short finding>"
    }
  ],
  "cross_attempt_reconciliation": "<short retry comparison or null>",
  "follow_up_candidates": ["<tool-call-part-id>"]
}