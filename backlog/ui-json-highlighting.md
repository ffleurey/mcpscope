Several places in the UI we have the possibility to inspect JSON configuration or payloads.

We have 2 issues with the readability:
* highlighting
* word wrapping

In the same way as we have markdown rendering in the chat UI, we should consider adding JSON highlighting and word wrapping support.
